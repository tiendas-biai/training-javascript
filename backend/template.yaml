AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Dev Drill - per-user progress API (Lambda + DynamoDB + Auth0 JWT authorizer)

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, prod]
    Description: Deployment environment

  Auth0Domain:
    Type: String
    Description: Auth0 tenant domain, e.g. dev-xxxx.us.auth0.com (no scheme, no trailing slash)

  Auth0Audience:
    Type: String
    Default: https://dev-drill-api
    Description: Auth0 API identifier (the access-token audience)

  AllowedOrigins:
    Type: CommaDelimitedList
    Default: "http://localhost:5173,https://training-javascript-one.vercel.app"
    Description: CORS allow-list for the HTTP API

  ProgressTableName:
    Type: String
    Default: drill-progress-dev
    Description: DynamoDB table name for per-user progress

  CardsTableName:
    Type: String
    Default: drill-cards-dev
    Description: DynamoDB table name for the question banks (read model)

Globals:
  Function:
    Runtime: nodejs24.x
    MemorySize: 256
    Timeout: 15

Resources:
  # Question banks (derived read model; JSON in app/data stays the source of truth).
  # PK subject + SK id, so one Query returns a whole subject's cards.
  CardsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref CardsTableName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: subject
          AttributeType: S
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: subject
          KeyType: HASH
        - AttributeName: id
          KeyType: RANGE

  # Per-card progress rows: PK userId (Auth0 sub) + SK cardKey ("<subject>#<cardId>").
  ProgressTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref ProgressTableName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
        - AttributeName: cardKey
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
        - AttributeName: cardKey
          KeyType: RANGE

  # HTTP API with an Auth0 JWT authorizer applied to every route by default.
  # The authorizer validates issuer + audience; the Lambda then trusts
  # requestContext.authorizer.jwt.claims.sub as the user id (never the body).
  DrillHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: !Ref Environment
      CorsConfiguration:
        AllowOrigins: !Ref AllowedOrigins
        AllowHeaders: [Content-Type, Authorization]
        AllowMethods: [GET, POST, PUT, DELETE, OPTIONS]
        MaxAge: 600
      Auth:
        Authorizers:
          Auth0JwtAuthorizer:
            IdentitySource: $request.header.Authorization
            JwtConfiguration:
              issuer: !Sub "https://${Auth0Domain}/"
              audience:
                - !Ref Auth0Audience
        DefaultAuthorizer: Auth0JwtAuthorizer

  ProgressFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: progress/
      Handler: index.handler
      Description: !Sub "Dev Drill per-user progress handler - ${Environment}"
      Environment:
        Variables:
          TABLE_NAME: !Ref ProgressTableName
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ProgressTableName
      Events:
        GetProgress:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /progress/{subject}
            Method: GET
        PutProgress:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /progress/{subject}/{cardId}
            Method: PUT
        ResetProgress:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /progress/{subject}
            Method: DELETE

  CardsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: cards/
      Handler: index.handler
      Description: !Sub "Dev Drill question banks handler - ${Environment}"
      Environment:
        Variables:
          TABLE_NAME: !Ref CardsTableName
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref CardsTableName
      Events:
        # Public read — no authorizer, so the app can load banks anonymously.
        ListCards:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /cards/{subject}
            Method: GET
            Auth:
              Authorizer: NONE
        # Writes require a valid token; the Lambda also checks the manage:cards
        # permission claim before mutating.
        CreateCard:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /cards/{subject}
            Method: POST
        UpdateCard:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /cards/{subject}/{cardId}
            Method: PUT
        DeleteCard:
          Type: HttpApi
          Properties:
            ApiId: !Ref DrillHttpApi
            Path: /cards/{subject}/{cardId}
            Method: DELETE

Outputs:
  ApiUrl:
    Description: Base URL for VITE_API_URL (local .env + Vercel env)
    Value: !Sub "https://${DrillHttpApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}"
  ProgressTableName:
    Description: DynamoDB progress table name
    Value: !Ref ProgressTableName
  CardsTableName:
    Description: DynamoDB cards table name (seed target)
    Value: !Ref CardsTableName
