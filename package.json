{
  "name": "training-javascript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "app": "npm run dev --prefix app",
    "app:build": "npm run build --prefix app",
    "app:test": "npm test --prefix app",
    "app:typecheck": "npm run typecheck --prefix app"
  },
  "private": true,
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
