{
  "docker-img-001": {
    "explanation": "**The problem:** Beginners conflate \"image\" and \"container\" and get confused about where data lives and why containers are disposable. **The distinction:** an *image* is a frozen, read-only template — a stack of filesystem layers plus metadata (env, entrypoint). A *container* is a running or stopped *instance* of that image: the shared read-only layers plus a thin **writable layer** unique to that container. **Why layers matter:** every Dockerfile instruction creates one content-addressed layer; identical layers are stored once and shared across images, so builds and pulls reuse what hasn't changed. **The consequence:** you can start ten containers from one image cheaply (they share the read-only layers), and anything written inside a container lives only in its throwaway writable layer — delete the container and that data is gone. **Common mistakes:** storing important data in the writable layer instead of a volume, and thinking each container copies the whole image (it doesn't — layers are shared copy-on-write).",
    "example": "```bash\n# One image...\ndocker pull nginx:1.27\ndocker image ls nginx        # REPOSITORY  TAG   IMAGE ID  SIZE\n\n# ...many containers, each with its own writable layer\ndocker run -d --name web1 nginx:1.27\ndocker run -d --name web2 nginx:1.27\ndocker ps                    # two containers, same image\n\n# Inspect the layer history of the image\ndocker history nginx:1.27\n```",
    "resources": [
      { "label": "Docker docs — Images and layers", "url": "https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-an-image/" },
      { "label": "Docker docs — What is a container?", "url": "https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/" }
    ]
  },
  "docker-img-002": {
    "explanation": "**The problem:** A tiny one-character source edit triggers a full `npm install` on every build, wasting minutes. **Why it happens:** Docker caches each instruction as a layer and reuses it only if that instruction *and every layer before it* is unchanged. The moment a layer is invalidated, **all subsequent layers rebuild** — cache invalidation cascades downward. **Why ordering fixes it:** copy and install dependencies *before* copying the rest of the source, so the expensive install layer only busts when the dependency manifest actually changes, not on every code edit. **How COPY invalidates:** `COPY`/`ADD` layers key off file *content* (a checksum), so editing any copied file invalidates that layer and everything after it. **Common mistakes:** putting `COPY . .` near the top (every edit re-runs install), and not using a `.dockerignore` so unrelated files (`.git`, `node_modules`) bust the cache and bloat the build context.",
    "example": "```dockerfile\nFROM node:20-slim\nWORKDIR /app\n\n# 1) Copy ONLY the manifest first — this layer rarely changes\nCOPY package.json package-lock.json ./\nRUN npm ci                       # cached unless deps change\n\n# 2) Copy the rest AFTER — editing source won't bust npm ci\nCOPY . .\n\nCMD [\"node\", \"server.js\"]\n```",
    "resources": [
      { "label": "Docker docs — Build cache", "url": "https://docs.docker.com/build/cache/" },
      { "label": "Docker docs — Dockerfile best practices", "url": "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/" }
    ]
  },
  "docker-img-003": {
    "explanation": "**The problem:** A naive image bundles compilers, dev dependencies, and source code into production — hundreds of MB of attack surface you never run. **The solution:** multi-stage builds use several `FROM` stages in one Dockerfile; an early \"builder\" stage compiles/installs everything, and the final stage starts from a minimal base and `COPY --from=builder` only the finished artifacts. **Why it works:** only the *last* stage becomes the published image — the builder stage and its tooling are discarded, so the result carries just the runtime and your built output. **The payoff:** images shrink from hundreds of MB to tens, builds stay fast (each stage is cached independently), and there's no compiler or source for an attacker to use. **Common mistakes:** forgetting `--from` and re-running the build in the final stage, copying the whole builder filesystem instead of just the artifact, and not naming stages (`AS builder`) so `COPY --from` references are brittle.",
    "example": "```dockerfile\n# Stage 1: build (heavy — has the full toolchain)\nFROM node:20 AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build                # produces /app/dist\n\n# Stage 2: runtime (tiny — no build tools, no source)\nFROM nginx:1.27-alpine\nCOPY --from=builder /app/dist /usr/share/nginx/html\nEXPOSE 80\n```",
    "resources": [
      { "label": "Docker docs — Multi-stage builds", "url": "https://docs.docker.com/build/building/multi-stage/" }
    ]
  },
  "docker-img-mcq-001": {
    "explanation": "**The problem:** People use `ENTRYPOINT` and `CMD` interchangeably and are surprised when `docker run` arguments behave unexpectedly. **The model:** the container's final command is `ENTRYPOINT + CMD` concatenated. `ENTRYPOINT` is the executable that *always* runs; `CMD` provides *default arguments* (or a default command if there's no ENTRYPOINT). **Why it matters:** arguments you pass to `docker run img ...` **replace CMD** but are appended to ENTRYPOINT — so ENTRYPOINT makes a container behave like a single binary with overridable flags. **Override rules:** `docker run img --flag` swaps CMD's defaults; to replace the executable itself you need `--entrypoint`. **Exec vs shell form:** always prefer the JSON-array \"exec\" form (`[\"app\"]`) — the \"shell\" form (`app`) wraps the process in `/bin/sh -c`, which swallows Unix signals so your app never receives SIGTERM on shutdown. **Common mistakes:** using shell form and then wondering why `docker stop` takes 10s (SIGKILL fallback), and putting the whole command in CMD when you wanted a fixed entrypoint.",
    "example": "```dockerfile\nFROM alpine:3.20\n# Always runs `ping`; CMD gives a default target you can override\nENTRYPOINT [\"ping\"]\nCMD [\"localhost\"]\n# docker run img            -> ping localhost\n# docker run img example.com -> ping example.com  (CMD replaced)\n# docker run --entrypoint sh img  -> sh           (ENTRYPOINT replaced)\n```",
    "resources": [
      { "label": "Docker docs — ENTRYPOINT", "url": "https://docs.docker.com/reference/dockerfile/#entrypoint" },
      { "label": "Docker docs — CMD", "url": "https://docs.docker.com/reference/dockerfile/#cmd" }
    ]
  },
  "docker-img-mcq-002": {
    "explanation": "**The problem:** `ADD` and `COPY` look interchangeable, and `ADD`'s hidden magic causes surprises. **What COPY does:** copies files/directories from the build context into the image — nothing more, which is exactly why it's predictable. **What ADD does extra:** it can also fetch a remote **URL** and **auto-extract a local tar archive** into the destination. **Why prefer COPY:** the Dockerfile best practices explicitly recommend `COPY` for plain copies because `ADD`'s auto-extraction and URL fetching are easy to trigger by accident (a `.tar` you meant to keep intact gets unpacked) and make builds less obvious. **When ADD earns its place:** unpacking a local tarball you control, where the extraction is the point. **Common mistakes:** using `ADD` to download a URL (use a `RUN curl` with a checksum instead, so you can verify and clean up in one layer), and assuming `ADD` is the newer/better default — it's the opposite.",
    "example": "```dockerfile\nFROM debian:12-slim\nWORKDIR /app\n\n# Predictable: just copy files\nCOPY app/ ./app/\n\n# Legitimate ADD use: auto-extract a local tarball into /opt\nADD release.tar.gz /opt/\n\n# Prefer this over `ADD <url>` so you can verify + clean up:\n# RUN curl -fsSL https://example.com/tool.tgz -o /tmp/t.tgz && tar -xzf /tmp/t.tgz -C /opt && rm /tmp/t.tgz\n```",
    "resources": [
      { "label": "Docker docs — ADD vs COPY best practice", "url": "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/#add-or-copy" }
    ]
  },
  "docker-img-004": {
    "explanation": "**The problem:** A deployment that worked yesterday breaks today even though \"nothing changed\" — because it pulled `:latest` and the tag now points to different bytes. **Tags are mutable:** a tag like `myapp:1.2` or `:latest` is just a *name* pointing at an image; whoever controls the repo can re-point it. `:latest` is merely the default tag, not a promise of newness. **Digests are immutable:** `myapp@sha256:abc…` is the cryptographic hash of the image content — it can only ever refer to those exact bytes. **Why pin:** referencing a digest (or at minimum a specific immutable version tag) gives reproducible, tamper-evident deploys; Kubernetes/ECS pull exactly what you tested. **Registry help:** ECR and others offer *tag immutability* so a tag, once pushed, can't be overwritten. **Common mistakes:** running `:latest` in production, assuming a re-pull of the same tag is idempotent, and pinning a tag in Git but letting CI overwrite that tag in the registry.",
    "example": "```bash\n# Tag: mutable pointer\ndocker pull nginx:1.27\n\n# Find the immutable digest of what you just pulled\ndocker inspect --format='{{index .RepoDigests 0}}' nginx:1.27\n# nginx@sha256:1234...\n\n# Pin the digest for reproducible deploys (k8s image: / ECS image:)\ndocker pull nginx@sha256:1234abcd...   # always the exact same bytes\n```",
    "resources": [
      { "label": "Docker docs — Image tag reference", "url": "https://docs.docker.com/reference/cli/docker/image/tag/" },
      { "label": "Amazon ECR — Image tag mutability", "url": "https://docs.aws.amazon.com/AmazonECR/latest/userguide/image-tag-mutability.html" }
    ]
  },
  "docker-run-001": {
    "explanation": "**The problem:** Newcomers expect `docker run` to resume an existing container and are confused by a pile of stopped containers from `docker ps -a`. **The lifecycle:** a container moves through created → running → stopped/exited → removed. `docker run` is shorthand for **create + start** of a *brand new* container each time. **stop vs rm:** `docker stop` sends SIGTERM (then SIGKILL after a grace period) — the container exits but still exists with its writable layer; `docker rm` permanently deletes a stopped container. **Restarting:** `docker start <name>` re-runs a stopped container (same writable layer); only `docker ps -a` shows stopped ones. **Why data vanishes:** removing a container discards its writable layer, so persistent data must live in a volume. **Common mistakes:** running `docker run` repeatedly (spawning new containers) when you meant `docker start`, forgetting `--rm` and accumulating dead containers, and keeping state in the container instead of a volume.",
    "example": "```bash\ndocker run -d --name api myimage     # create + start (new container)\ndocker stop api                      # SIGTERM, then exits (still exists)\ndocker ps -a | grep api              # shows it as Exited\ndocker start api                     # restart the SAME container\ndocker rm -f api                     # force-stop and delete it\n\ndocker run --rm myimage some-task    # auto-removed when it exits\n```",
    "resources": [
      { "label": "Docker docs — docker run", "url": "https://docs.docker.com/reference/cli/docker/container/run/" },
      { "label": "Docker docs — Container lifecycle basics", "url": "https://docs.docker.com/get-started/docker-concepts/running-containers/" }
    ]
  },
  "docker-run-002": {
    "explanation": "**The problem:** You need a shell inside a running container to debug, but `docker run` keeps creating new ones. **The fix:** `docker exec` starts an *additional* process inside an already-running container, reusing its namespaces — `docker exec -it <name> sh` drops you into a shell. **What the flags mean:** `-d` (detached) runs the container in the background and returns immediately; `-i` keeps STDIN open; `-t` allocates a pseudo-TTY; `-it` together give a usable interactive terminal. **exec vs attach:** `exec` launches a new process (safe to exit without killing the container); `attach` connects to the container's main process (Ctrl-C may stop it). **Reading output without a shell:** `docker logs <name>` streams the main process's stdout/stderr. **Common mistakes:** running `docker exec` on a stopped container (it must be running), forgetting `-t` and getting a broken prompt, and using `attach` then accidentally killing the app with Ctrl-C.",
    "example": "```bash\ndocker run -d --name api myimage          # detached, runs in background\n\ndocker exec -it api sh                     # interactive shell inside it\n# / # ls /app   ... poke around, then `exit` (container keeps running)\n\ndocker exec api cat /etc/os-release        # one-off command, no TTY\ndocker logs -f api                          # follow stdout/stderr\n```",
    "resources": [
      { "label": "Docker docs — docker exec", "url": "https://docs.docker.com/reference/cli/docker/container/exec/" }
    ]
  },
  "docker-run-mcq-001": {
    "explanation": "**The problem:** A container should come back after a crash or a host reboot, but not when *you* deliberately stopped it — and the wrong policy gets one of those cases wrong. **The four policies:** `no` (default, never restart); `on-failure[:N]` (restart only on a non-zero exit, optionally capped at N tries); `always` (restart on any exit *and* on daemon start, even if you manually stopped it — a manual stop is undone the next time the daemon restarts); `unless-stopped` (same as `always`, but a manual `docker stop` is remembered and it stays down). **Why `unless-stopped` is usually right** for long-running services on a single host: it survives crashes and reboots yet respects an intentional stop. **Bigger picture:** under Kubernetes or ECS you don't set Docker restart policies — the orchestrator's controller restarts/reschedules failed tasks (Kubernetes Pods default to `restartPolicy: Always` for Deployments). **Common mistakes:** using `always` and being surprised a manually stopped container resurrects after reboot, and relying on `on-failure` for a process that exits 0 on crash.",
    "example": "```bash\n# Survive crashes and host reboots, but honor a manual stop:\ndocker run -d --restart unless-stopped --name api myimage\n\n# Retry up to 5 times only on non-zero exit:\ndocker run -d --restart on-failure:5 --name worker myimage\n\ndocker inspect -f '{{.HostConfig.RestartPolicy.Name}}' api   # unless-stopped\n```",
    "resources": [
      { "label": "Docker docs — Restart policies", "url": "https://docs.docker.com/config/containers/start-containers-automatically/" }
    ]
  },
  "docker-run-003": {
    "explanation": "**The problem:** Hardcoding config or writing logs to files inside the image makes a container that only works in one environment and hides its logs. **Config via environment:** pass settings with `-e KEY=value` or a `--env-file`, and mount config files as volumes — so the *same image* runs in dev, Kubernetes, and ECS with only the injected values changing (the 12-factor approach). **Logs to stdout/stderr:** a containerized process should treat logs as an event stream written to stdout/stderr, not to files it manages; Docker captures those streams (`docker logs`) and orchestrators forward them to CloudWatch, Loki, etc. **Why this matters:** stateless, externally-configured containers are what make them horizontally scalable and portable across orchestrators. **Secrets:** never bake secrets into the image or commit them in env files — inject them from a secret store (Kubernetes Secrets, AWS Secrets Manager/SSM) at runtime. **Common mistakes:** writing logs to a file inside the container (lost on restart, invisible to log collectors) and putting secrets in the Dockerfile (they persist in image layers).",
    "example": "```bash\n# Inject config without changing the image\ndocker run -d --name api \\\n  -e NODE_ENV=production \\\n  -e DB_HOST=db.internal \\\n  --env-file ./prod.env \\\n  myimage\n\n# The app logs to stdout/stderr -> Docker captures them:\ndocker logs -f api\n```",
    "resources": [
      { "label": "Docker docs — Set environment variables", "url": "https://docs.docker.com/reference/cli/docker/container/run/#env" },
      { "label": "The Twelve-Factor App — Config & Logs", "url": "https://12factor.net/config" }
    ]
  },
  "docker-net-001": {
    "explanation": "**The problem:** Data written inside a container disappears when the container is removed, and people don't know which storage option to reach for. **The three options:** a **volume** is storage Docker manages (under `/var/lib/docker/volumes`), the recommended way to persist data — portable, backup-friendly, and independent of the host's directory layout. A **bind mount** maps a specific *host path* into the container, ideal for development (edit source on the host, see it live in the container). **tmpfs** lives only in RAM and vanishes when the container stops — for secrets or scratch space you never want on disk. **Why not the writable layer:** the container's own writable layer is ephemeral and tied to the container's life, so durable data must go to a volume. **Why volumes over bind mounts in prod:** volumes don't depend on host paths existing and are managed/portable; bind mounts couple you to the host filesystem. **Common mistakes:** persisting to the writable layer and losing data on `docker rm`, and using bind mounts in production where the host path may differ.",
    "example": "```bash\n# Named volume — Docker-managed, survives container removal\ndocker volume create pgdata\ndocker run -d --name db -v pgdata:/var/lib/postgresql/data postgres:16\ndocker rm -f db                      # data stays in the volume\n\n# Bind mount — live host source for dev\ndocker run --rm -v \"$PWD\":/app -w /app node:20 npm test\n\n# tmpfs — in-memory only\ndocker run --rm --tmpfs /cache alpine ls -ld /cache\n```",
    "resources": [
      { "label": "Docker docs — Manage data / volumes", "url": "https://docs.docker.com/storage/volumes/" },
      { "label": "Docker docs — Bind mounts", "url": "https://docs.docker.com/storage/bind-mounts/" }
    ]
  },
  "docker-net-002": {
    "explanation": "**The problem:** Two containers can't reach each other by name on the default network, and people don't know which network driver to use. **bridge (default):** each standalone container joins a private bridge network and reaches the internet via NAT; ports must be published with `-p` to be reachable from the host. **The DNS catch:** the *built-in* `bridge` network does **not** give name resolution — only a **user-defined** bridge network (or Compose, which makes one for you) runs an embedded DNS server so containers resolve each other by **container/service name**. **host:** the container shares the host's network namespace directly — no isolation and no `-p` mapping (the container's ports *are* the host's). **none:** networking disabled entirely. **Why it scales up:** name-based service discovery on a user-defined network is the same concept Kubernetes Services and ECS service discovery provide across many hosts. **Common mistakes:** expecting DNS on the default bridge, and using `host` networking just to avoid port mapping (it breaks isolation and portability).",
    "example": "```bash\n# Create a user-defined network so name DNS works\ndocker network create appnet\n\ndocker run -d --name db   --network appnet postgres:16\ndocker run -d --name api  --network appnet -p 8080:8080 myimage\n\n# 'api' can now reach the database at host 'db' (DNS by container name)\ndocker exec api getent hosts db        # resolves to db's container IP\n```",
    "resources": [
      { "label": "Docker docs — Networking overview", "url": "https://docs.docker.com/network/" },
      { "label": "Docker docs — Bridge networks & DNS", "url": "https://docs.docker.com/network/drivers/bridge/" }
    ]
  },
  "docker-net-mcq-001": {
    "explanation": "**The problem:** People reverse the port mapping order or expect `EXPOSE` alone to make a service reachable. **The rule:** `-p hostPort:containerPort` publishes a container's port onto the host. So `-p 8080:80` means traffic arriving at the *host's* 8080 is forwarded to the *container's* 80 — visit `localhost:8080` to hit nginx. **The order matters:** it's always `host:container`; swapping them silently maps the wrong ports. **EXPOSE is documentation only:** the `EXPOSE` instruction in a Dockerfile records which ports the app listens on but publishes nothing — you still need `-p` at run time (or `-P` to auto-publish all EXPOSEd ports to random host ports). **Bind address:** you can restrict to an interface, e.g. `-p 127.0.0.1:8080:80` to publish only on localhost. **Bigger picture:** orchestrators replace manual `-p` with Services (Kubernetes) or task/port mappings + load balancers (ECS). **Common mistakes:** writing `-p 80:8080` when you meant the reverse, and assuming `EXPOSE` opens the port.",
    "example": "```bash\n# Host 8080 -> container 80\ndocker run -d -p 8080:80 nginx\ncurl localhost:8080                 # reaches nginx on port 80\n\n# Publish only on localhost\ndocker run -d -p 127.0.0.1:5432:5432 postgres:16\n\n# -P auto-publishes every EXPOSEd port to a random host port\ndocker run -d -P nginx && docker port $(docker ps -lq)\n```",
    "resources": [
      { "label": "Docker docs — Publish ports", "url": "https://docs.docker.com/network/#published-ports" }
    ]
  },
  "docker-cmp-001": {
    "explanation": "**The problem:** A real app is several containers (API, DB, cache) and starting each with the right flags, network, and order by hand is tedious and error-prone. **What Compose does:** you declare all services, networks, and volumes in one `docker-compose.yml` and bring the whole stack up with `docker compose up` (and down with `docker compose down`). **Service discovery for free:** Compose creates a user-defined network so each service reaches the others by **service name** (the `api` service connects to `db` at host `db`). **Ordering & config:** `depends_on` controls start order, and env/volumes are declared per service — reproducible environments from one file. **Where it stops:** Compose is built for local dev and integration tests on a single host; it isn't a production orchestrator — for multi-node scaling, self-healing, and rolling updates you graduate to Kubernetes or ECS, but the service-oriented model carries over. **Common mistakes:** relying on `depends_on` for readiness (it waits for *start*, not *ready* — add a healthcheck), and trying to run Compose as your production cluster.",
    "example": "```yaml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8080:8080\"\n    environment:\n      DB_HOST: db          # reach the db service by name\n    depends_on:\n      - db\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_PASSWORD: secret\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n\nvolumes:\n  pgdata:\n```",
    "resources": [
      { "label": "Docker docs — Compose overview", "url": "https://docs.docker.com/compose/" },
      { "label": "Docker docs — Compose file reference", "url": "https://docs.docker.com/reference/compose-file/" }
    ]
  },
  "docker-reg-001": {
    "explanation": "**The problem:** You built an image locally but teammates and clusters can't get it — it needs to live in a registry. **What a registry is:** a service that stores and serves images (Docker Hub, Amazon ECR, GitHub Container Registry, …). **The reference format:** `registry/namespace/repository:tag`, e.g. `123456789012.dkr.ecr.us-east-1.amazonaws.com/myapp:1.2.0`; omit the registry and Docker defaults to **Docker Hub** (`nginx` is really `docker.io/library/nginx`). **The workflow:** `docker tag` re-labels a local image with the destination repo, `docker push` uploads it, `docker pull` retrieves it elsewhere — layers already present are skipped, so transfers are incremental. **Auth & existence:** private registries need `docker login`, and some (notably ECR) require the repository to be **created first** before a push succeeds. **Common mistakes:** forgetting to `docker tag` with the full registry path before pushing (it tries Docker Hub), and pushing to an ECR repo that doesn't exist yet.",
    "example": "```bash\n# Build, then tag with the full destination reference\ndocker build -t myapp:1.2.0 .\ndocker tag myapp:1.2.0 123456789012.dkr.ecr.us-east-1.amazonaws.com/myapp:1.2.0\n\n# Push to the registry (after docker login)\ndocker push 123456789012.dkr.ecr.us-east-1.amazonaws.com/myapp:1.2.0\n\n# Anywhere else: pull it back\ndocker pull 123456789012.dkr.ecr.us-east-1.amazonaws.com/myapp:1.2.0\n```",
    "resources": [
      { "label": "Docker docs — docker push", "url": "https://docs.docker.com/reference/cli/docker/image/push/" },
      { "label": "Docker docs — What is a registry?", "url": "https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-registry/" }
    ]
  },
  "docker-reg-mcq-001": {
    "explanation": "**The problem:** A `docker push` to a private repo fails with 'unauthorized' because the CLI has no credentials. **The fix:** run `docker login <registry>` first — it authenticates and caches a token in your Docker config (`~/.docker/config.json`), so subsequent `push`/`pull` to that registry are authorized without re-entering credentials. **Do it securely:** never put a password on the command line (it lands in shell history); pipe it via `--password-stdin`. **There is no Dockerfile login:** authentication is a CLI/daemon concern, not a build instruction — `LOGIN` doesn't exist. **ECR specifics:** Amazon ECR doesn't use a static password; you fetch a short-lived (12-hour) token with `aws ecr get-login-password` and pipe it into `docker login --password-stdin` against your account's ECR endpoint (covered in the AWS topics). **Common mistakes:** typing the password inline instead of `--password-stdin`, logging in to Docker Hub when you meant a private registry (wrong/blank registry argument), and assuming push auto-prompts for credentials.",
    "example": "```bash\n# Generic private registry / Docker Hub\necho \"$REGISTRY_TOKEN\" | docker login registry.example.com -u myuser --password-stdin\n\n# Amazon ECR: short-lived token, piped to docker login\naws ecr get-login-password --region us-east-1 \\\n  | docker login --username AWS --password-stdin \\\n    123456789012.dkr.ecr.us-east-1.amazonaws.com\n\ndocker push 123456789012.dkr.ecr.us-east-1.amazonaws.com/myapp:1.2.0\n```",
    "resources": [
      { "label": "Docker docs — docker login", "url": "https://docs.docker.com/reference/cli/docker/login/" },
      { "label": "Amazon ECR — Registry authentication", "url": "https://docs.aws.amazon.com/AmazonECR/latest/userguide/registry_auth.html" }
    ]
  },
  "docker-k8s-pod-001": {
    "explanation": "**The problem:** Coming from Docker, you expect to deploy 'a container', but Kubernetes never schedules a bare container — it schedules a Pod. **What a Pod is:** the smallest deployable unit, a wrapper around one or more containers that share a network namespace (a single Pod IP, reachable from each other over `localhost`) and can share volumes. **Why group containers:** the main app container plus tightly-coupled helpers (a log shipper, a proxy) that must live and die together and share local resources — the *sidecar* pattern. **The usual shape:** one app container per Pod; reach for multiple only when they're genuinely inseparable. **Why you don't create Pods directly:** a bare Pod isn't rescheduled if its node dies — a controller (Deployment) owns Pods and recreates them. **Common mistakes:** stuffing unrelated services into one Pod (they can't scale independently), and assuming containers in different Pods can talk over `localhost` (they can't — they have separate IPs).",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\n  labels:\n    app: web\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      ports:\n        - containerPort: 8080\n    - name: log-shipper          # sidecar: shares network + volume\n      image: fluent/fluent-bit:3.0\n```",
    "resources": [
      { "label": "Kubernetes docs — Pods", "url": "https://kubernetes.io/docs/concepts/workloads/pods/" }
    ]
  },
  "docker-k8s-pod-002": {
    "explanation": "**The problem:** You want N copies of your app to stay running, self-heal, and update without downtime — managing Pods by hand can't do that. **The three layers:** a *Pod* runs your containers; a *ReplicaSet* is a controller that keeps exactly N identical Pods alive (recreating any that die); a *Deployment* sits on top and manages ReplicaSets to give you declarative rollouts and rollbacks. **How it reconciles:** you declare desired state (image, `replicas: 3`) in the Deployment, and the control loop continuously makes reality match — losing a Pod triggers the ReplicaSet to recreate it. **Why the extra ReplicaSet layer:** on each change the Deployment creates a *new* ReplicaSet and scales it up while scaling the old one down, which is what enables zero-downtime rollouts and one-command rollback. **Common mistakes:** creating ReplicaSets or Pods directly (you lose rollout/rollback), and editing Pods in place expecting the change to stick (the controller will revert/recreate them).",
    "example": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: web\n  template:\n    metadata:\n      labels:\n        app: web\n    spec:\n      containers:\n        - name: app\n          image: myrepo/app:1.2.0\n```",
    "resources": [
      { "label": "Kubernetes docs — Deployments", "url": "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/" }
    ]
  },
  "docker-k8s-pod-mcq-001": {
    "explanation": "**The problem:** You ship a new image and need it live without dropping requests — a naive 'stop all, start all' causes an outage. **What RollingUpdate does:** the default Deployment strategy creates a new ReplicaSet and shifts Pods over incrementally, adding new Pods and retiring old ones a few at a time so capacity stays available throughout. **The knobs:** `maxSurge` (how many extra Pods above desired count may be created) and `maxUnavailable` (how many may be down) bound the pace and the risk. **Rollback:** because the old ReplicaSet is kept (scaled to zero), `kubectl rollout undo` instantly reverts. **The alternative:** the `Recreate` strategy terminates all old Pods before creating new ones — accepting downtime — and is only appropriate when two versions can't coexist (e.g. an incompatible schema/lock). **Common mistakes:** assuming a rollout restarts Pods in place (it replaces them), and not pairing rollouts with readiness probes — without readiness, traffic hits new Pods before they're ready.",
    "example": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  replicas: 4\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1            # at most 1 extra Pod during rollout\n      maxUnavailable: 0      # never drop below desired capacity\n  selector:\n    matchLabels: { app: web }\n  template:\n    metadata:\n      labels: { app: web }\n    spec:\n      containers:\n        - name: app\n          image: myrepo/app:1.3.0   # change this -> rolling update\n```",
    "resources": [
      { "label": "Kubernetes docs — Rolling updates", "url": "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#rolling-update-deployment" }
    ]
  },
  "docker-k8s-pod-003": {
    "explanation": "**The problem:** A Pod won't start with `ImagePullBackOff`, or it keeps running a stale image — both come from misunderstanding how images are referenced and pulled. **Where the image lives:** each entry in `spec.containers` has an `image:` field with the full reference (`registry/repo:tag` or `@sha256:digest`). **Re-pull behavior:** `imagePullPolicy` decides whether the kubelet re-pulls — `Always` pulls every start, `IfNotPresent` uses the node's cache, `Never` requires it pre-loaded; the default is `Always` for the `:latest` tag and `IfNotPresent` otherwise. **Private registries:** the kubelet needs credentials, supplied via an `imagePullSecrets` reference on the Pod/ServiceAccount. **Why pin a real tag/digest:** `:latest` makes `imagePullPolicy` default to `Always` and makes rollouts non-reproducible. **EKS shortcut:** nodes can pull from ECR using the node IAM role, so you often need no imagePullSecret at all (AWS topics). **Common mistakes:** relying on `:latest` then being unable to tell which build is running, and forgetting `imagePullSecrets` for a private repo (→ `ImagePullBackOff`).",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  imagePullSecrets:\n    - name: regcred           # docker-registry secret for a private repo\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0  # pin a tag (or @sha256:...) not :latest\n      imagePullPolicy: IfNotPresent\n```",
    "resources": [
      { "label": "Kubernetes docs — Images", "url": "https://kubernetes.io/docs/concepts/containers/images/" }
    ]
  },
  "docker-k8s-pod-004": {
    "explanation": "**The problem:** Your Service has zero endpoints, or a Deployment isn't managing the Pods you expect — almost always a label/selector mismatch. **How coupling works:** Kubernetes wires objects together not by names but by **labels** (key/value tags on objects) and **selectors** (queries over those labels). A Deployment's `spec.selector.matchLabels` must match the labels on its Pod `template`; a Service's `spec.selector` chooses which Pods get its traffic. **Why this design:** loose coupling means a Service automatically routes to brand-new Pods created during a rollout — they carry the matching label, so they join the endpoint set with no edit to the Service. **The gotcha:** a Deployment's selector is immutable and must match the template labels exactly, or the Deployment is rejected/orphans Pods. **Common mistakes:** Service selector not matching Pod labels (→ no endpoints, connection refused), and reusing the same labels across unrelated Deployments so a Service grabs the wrong Pods.",
    "example": "```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: web\nspec:\n  selector:\n    app: web              # routes to every Pod labeled app=web\n  ports:\n    - port: 80\n      targetPort: 8080\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  selector:\n    matchLabels: { app: web }\n  template:\n    metadata:\n      labels: { app: web }   # must match both selectors above\n    spec:\n      containers:\n        - name: app\n          image: myrepo/app:1.2.0\n```",
    "resources": [
      { "label": "Kubernetes docs — Labels and selectors", "url": "https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/" }
    ]
  },
  "docker-k8s-svc-001": {
    "explanation": "**The problem:** Pods are ephemeral with ever-changing IPs, so clients can't depend on a Pod IP — you need a stable front. **What a Service is:** a stable virtual IP + DNS name that load-balances across the current set of Pods matching its selector. **The three common types:** `ClusterIP` (default) is in-cluster only; `NodePort` opens the same fixed port on every node so outside traffic can reach the Service; `LoadBalancer` asks the cloud to provision an external load balancer (an ELB/NLB on AWS) pointing at the Service. **How they layer:** LoadBalancer is built on NodePort, which is built on ClusterIP — each adds reach. **When to use which:** ClusterIP for internal services, LoadBalancer to expose one service externally; for many HTTP routes, use a single Ingress instead of many LoadBalancers (cost + management). **Common mistakes:** creating a LoadBalancer per microservice (a pile of paid load balancers) when an Ingress would consolidate them, and expecting a ClusterIP to be reachable from outside the cluster.",
    "example": "```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: web\nspec:\n  type: LoadBalancer       # ClusterIP | NodePort | LoadBalancer\n  selector:\n    app: web\n  ports:\n    - port: 80             # the Service port\n      targetPort: 8080     # the container port\n```",
    "resources": [
      { "label": "Kubernetes docs — Service", "url": "https://kubernetes.io/docs/concepts/services-networking/service/" }
    ]
  },
  "docker-k8s-svc-mcq-001": {
    "explanation": "**The problem:** Picking the wrong Service type either needlessly exposes an internal service to the internet or fails to expose one that needs to be public. **ClusterIP:** the default, reachable only from inside the cluster — exactly right for a database, cache, or backend API that only other Pods call. **NodePort:** opens a fixed high port on every node, exposing the Service externally (rarely used directly in cloud setups). **LoadBalancer:** provisions a cloud load balancer for external access. **ExternalName:** a DNS-only alias (CNAME) to an out-of-cluster host — it proxies no traffic and selects no Pods. **The principle of least exposure:** keep internal services on ClusterIP and front only the things that must be public with LoadBalancer/Ingress. **Common mistakes:** using NodePort/LoadBalancer for internal-only services (unnecessary exposure and cost), and thinking ClusterIP means 'no networking' — it's fully reachable, just only from within the cluster.",
    "example": "```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: internal-db\nspec:\n  type: ClusterIP          # default — in-cluster only\n  selector:\n    app: db\n  ports:\n    - port: 5432\n      targetPort: 5432\n```",
    "resources": [
      { "label": "Kubernetes docs — Publishing Services (ServiceTypes)", "url": "https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types" }
    ]
  },
  "docker-k8s-svc-002": {
    "explanation": "**The problem:** Exposing ten HTTP services each via a LoadBalancer Service means ten cloud load balancers — expensive and unmanageable, with no shared TLS or path routing. **What Ingress adds:** an Ingress is a set of L7 (HTTP/HTTPS) rules mapping hostnames and URL paths to backend Services, with optional TLS termination — so many services share one external entry point and one load balancer. **The crucial catch:** an Ingress object is just data; it does nothing until an **ingress controller** is running to watch Ingress resources and program the real proxy/load balancer. **Controllers:** NGINX Ingress in-cluster, or the **AWS Load Balancer Controller** which turns an Ingress into an Application Load Balancer (ALB) on EKS. **Why it matters:** Ingress consolidates routing, TLS, and host/path rules behind a single LB. **Common mistakes:** creating an Ingress with no controller installed (nothing happens), and using path rules without setting the right `pathType`/rewrite for the controller.",
    "example": "```yaml\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: site\nspec:\n  rules:\n    - host: shop.example.com\n      http:\n        paths:\n          - path: /api\n            pathType: Prefix\n            backend:\n              service:\n                name: api\n                port:\n                  number: 80\n          - path: /\n            pathType: Prefix\n            backend:\n              service:\n                name: web\n                port:\n                  number: 80\n```",
    "resources": [
      { "label": "Kubernetes docs — Ingress", "url": "https://kubernetes.io/docs/concepts/services-networking/ingress/" }
    ]
  },
  "docker-k8s-svc-003": {
    "explanation": "**The problem:** Pods come and go with new IPs, so hardcoding addresses is hopeless — clients need names. **How discovery works:** Kubernetes runs an in-cluster DNS server (CoreDNS) that gives every Service a stable DNS name `<service>.<namespace>.svc.cluster.local`. **The shortcuts:** a Pod in the same namespace can use just `<service>`; from another namespace use `<service>.<namespace>`. **What it resolves to:** the Service's stable ClusterIP, which then load-balances to the live Pods behind it — so clients never deal with Pod IPs. **Why it scales:** this is the cluster-wide version of Docker's user-defined-network DNS; because everything addresses Services by name, Pods can be rescheduled, scaled, and replaced freely. **Common mistakes:** addressing Pods directly by IP (they change), forgetting the namespace qualifier for cross-namespace calls, and assuming a Service name resolves before the Service exists (create dependencies first or handle retries).",
    "example": "```bash\n# From a Pod, reach Services by DNS name (CoreDNS resolves these):\ncurl http://api/healthz                       # same namespace\ncurl http://api.backend/healthz               # other namespace 'backend'\ncurl http://api.backend.svc.cluster.local     # fully qualified\n\nkubectl exec -it web -- nslookup api          # see it resolve to the ClusterIP\n```",
    "resources": [
      { "label": "Kubernetes docs — DNS for Services and Pods", "url": "https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/" }
    ]
  },
  "docker-k8s-cfg-001": {
    "explanation": "**The problem:** Hardcoding config in the image means rebuilding to change a setting and shipping a different image per environment. **What a ConfigMap is:** a cluster object holding non-confidential key/value config, decoupled from the image so one image runs everywhere with different config injected. **Two ways to consume it:** as environment variables (`envFrom` or `valueFrom.configMapKeyRef`) or as files in a mounted volume. **The update nuance:** env-var values are read once at container start, so changing the ConfigMap needs a Pod restart to take effect; a volume-mounted ConfigMap is updated in place by the kubelet, so an app that re-reads the file picks up changes live. **Not for secrets:** ConfigMaps are plain text — passwords/tokens belong in Secrets. **Common mistakes:** putting credentials in a ConfigMap, and expecting env-var config to update without a restart (use a volume mount if you need live reload).",
    "example": "```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: app-config\ndata:\n  LOG_LEVEL: info\n  FEATURE_X: \"true\"\n---\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      envFrom:\n        - configMapRef:\n            name: app-config   # injects LOG_LEVEL, FEATURE_X as env vars\n```",
    "resources": [
      { "label": "Kubernetes docs — ConfigMaps", "url": "https://kubernetes.io/docs/concepts/configuration/configmap/" }
    ]
  },
  "docker-k8s-cfg-002": {
    "explanation": "**The problem:** People store passwords in a Secret, see base64, and assume it's encrypted — it isn't. **What a Secret is:** like a ConfigMap but intended for sensitive data, consumed the same way (env vars or mounted files). **The base64 truth:** Secret values are base64-*encoded*, which is just reversible encoding for transport of binary data — anyone with read access (or the etcd data) can decode it. **Making it actually secure:** enable **encryption at rest** for Secrets in etcd, lock down access with RBAC, and avoid committing Secret manifests to Git. **The AWS-native approach:** keep secrets in AWS Secrets Manager / SSM Parameter Store and pull them in at runtime (e.g. the Secrets Store CSI driver, or External Secrets) so they're never in plain manifests — covered in the AWS topics. **Common mistakes:** treating base64 as encryption, checking Secret YAML into version control, and granting broad `get secrets` RBAC.",
    "example": "```bash\n# base64 is ENCODING, not encryption — trivially reversible:\necho -n 's3cr3t' | base64           # czNjcjN0\necho 'czNjcjN0' | base64 --decode   # s3cr3t\n\n# Create a Secret without hand-writing base64:\nkubectl create secret generic db-cred \\\n  --from-literal=username=admin \\\n  --from-literal=password='s3cr3t'\n\n# Real protection: enable etcd encryption-at-rest + restrict RBAC\nkubectl get secret db-cred -o jsonpath='{.data.password}' | base64 -d\n```",
    "resources": [
      { "label": "Kubernetes docs — Secrets", "url": "https://kubernetes.io/docs/concepts/configuration/secret/" },
      { "label": "Kubernetes docs — Encrypting secret data at rest", "url": "https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/" }
    ]
  },
  "docker-k8s-cfg-mcq-001": {
    "explanation": "**The problem:** You update a ConfigMap expecting the running app to see the change, but env-var-based config doesn't budge. **Why env vars are static:** environment variables are materialized into the container's process when it starts, so a later ConfigMap edit has no effect until the Pod restarts — env injection is effectively a one-time snapshot. **Why volume mounts update:** a ConfigMap (or Secret) mounted as a volume is projected as files the kubelet refreshes in place when the source changes (after a short sync delay), so an app that re-reads the file picks up new values without a restart. **The tradeoff:** env vars are simpler and great for values that change only on deploy; volume mounts enable live reload but require the app to actually re-read the file. **The other options are fully static:** baking config into the image or passing it as command args can only change with a new image/Pod. **Common mistakes:** expecting `kubectl edit configmap` to live-update env-var config, and mounting as a volume but never re-reading the file in the app (so it still looks static).",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      volumeMounts:\n        - name: cfg\n          mountPath: /etc/app      # files update in place on ConfigMap change\n  volumes:\n    - name: cfg\n      configMap:\n        name: app-config\n```",
    "resources": [
      { "label": "Kubernetes docs — Mounted ConfigMaps are updated automatically", "url": "https://kubernetes.io/docs/concepts/configuration/configmap/#mounted-configmaps-are-updated-automatically" }
    ]
  },
  "docker-k8s-stor-001": {
    "explanation": "**The problem:** A container's filesystem is ephemeral, but databases and uploads must survive Pod rescheduling — you need storage decoupled from the Pod. **The supply/demand split:** a *PersistentVolume (PV)* is an actual piece of cluster storage (backed by EBS, EFS, NFS…), representing supply; a *PersistentVolumeClaim (PVC)* is a Pod's request for storage of a certain size and access mode, representing demand. **How they connect:** Kubernetes binds a PVC to a satisfying PV, and the Pod mounts the *PVC* — never the PV directly. **Why the indirection:** app authors ask for storage (PVC) without knowing the backend, while PVs (often created automatically via a StorageClass) encapsulate the real disk and its lifecycle. **Persistence:** the PVC outlives the Pod, so when a Pod is rescheduled the same data is re-attached. **Common mistakes:** mismatched access modes/size so a PVC stays `Pending` (no matching PV), and assuming `ReadWriteOnce` (EBS) can be mounted by Pods on multiple nodes (it can't — use EFS/`ReadWriteMany`).",
    "example": "```yaml\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: data\nspec:\n  accessModes:\n    - ReadWriteOnce        # single-node (e.g. EBS-backed)\n  storageClassName: gp3\n  resources:\n    requests:\n      storage: 10Gi\n---\napiVersion: v1\nkind: Pod\nmetadata:\n  name: db\nspec:\n  containers:\n    - name: pg\n      image: postgres:16\n      volumeMounts:\n        - name: data\n          mountPath: /var/lib/postgresql/data\n  volumes:\n    - name: data\n      persistentVolumeClaim:\n        claimName: data\n```",
    "resources": [
      { "label": "Kubernetes docs — Persistent Volumes", "url": "https://kubernetes.io/docs/concepts/storage/persistent-volumes/" }
    ]
  },
  "docker-k8s-stor-002": {
    "explanation": "**The problem:** Pre-creating a PersistentVolume for every PVC by hand doesn't scale and couples app teams to infrastructure details. **What a StorageClass is:** a named template describing a *kind* of storage — which CSI provisioner/driver to use and parameters like disk type, IOPS, filesystem, and reclaim policy. **Dynamic provisioning:** when a PVC references a StorageClass, Kubernetes automatically creates a matching PV (and the real underlying disk) on demand — no pre-provisioned PVs needed. **On EKS specifically:** a StorageClass backed by the **EBS CSI driver** provisions a block volume per PVC (`ReadWriteOnce`, single node); the **EFS CSI driver** provides shared `ReadWriteMany` storage many Pods/nodes can mount. **Why choose carefully:** the StorageClass picks backend, performance tier, and cost. **Common mistakes:** no default StorageClass set so PVCs hang `Pending`, expecting EBS volumes to be shared across nodes (use EFS for that), and a `Delete` reclaim policy silently destroying data when the PVC is removed.",
    "example": "```yaml\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: gp3\nprovisioner: ebs.csi.aws.com    # EKS EBS CSI driver -> dynamic EBS volumes\nparameters:\n  type: gp3\n  encrypted: \"true\"\nreclaimPolicy: Delete\nvolumeBindingMode: WaitForFirstConsumer\nallowVolumeExpansion: true\n```",
    "resources": [
      { "label": "Kubernetes docs — Storage Classes", "url": "https://kubernetes.io/docs/concepts/storage/storage-classes/" },
      { "label": "EKS — EBS CSI driver", "url": "https://docs.aws.amazon.com/eks/latest/userguide/ebs-csi.html" }
    ]
  },
  "docker-k8s-stor-mcq-001": {
    "explanation": "**The problem:** Teams use `emptyDir` for data they actually need to keep, then lose it when a Pod is rescheduled. **What emptyDir is:** an ephemeral volume created empty when a Pod is assigned to a node and deleted when the Pod is removed from that node — scratch space for caches, temp files, or sharing files between containers in the same Pod. **The lifetime rule:** it survives a *container* restart within the Pod, but is gone when the *Pod* is deleted or rescheduled — its lifetime is the Pod's, not the workload's. **What to use instead:** for data that must outlive the Pod, use a PVC backed by a PersistentVolume (EBS/EFS), which is re-attached to the rescheduled Pod. **The parallel:** this is exactly Docker's writable-layer (ephemeral) vs named-volume (persistent) distinction, one level up. **Common mistakes:** putting a database's data dir on `emptyDir`, and assuming `emptyDir` data moves with the Pod when it's rescheduled to another node (it doesn't).",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: cache-demo\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      volumeMounts:\n        - name: scratch\n          mountPath: /tmp/cache    # OK to lose on Pod deletion\n  volumes:\n    - name: scratch\n      emptyDir: {}                 # ephemeral — tied to the Pod's life\n```",
    "resources": [
      { "label": "Kubernetes docs — emptyDir", "url": "https://kubernetes.io/docs/concepts/storage/volumes/#emptydir" }
    ]
  },
  "docker-k8s-health-001": {
    "explanation": "**The problem:** Kubernetes can't tell a wedged container from a busy one, or a booting Pod from a ready one, unless you tell it — and using one probe for everything causes restart loops or traffic to dead Pods. **Liveness:** answers 'is this container healthy?' — on failure the kubelet **restarts** the container (recovers deadlocks). **Readiness:** answers 'can it serve requests right now?' — on failure the Pod is **removed from Service endpoints** so it gets no traffic, but is **not** restarted (lets it drain/warm up). **Startup:** answers 'has the app finished booting?' — it gates liveness/readiness so a slow start isn't mistaken for a failure and killed. **The mental model:** liveness restarts, readiness gates traffic, startup buys boot time. **Common mistakes:** an aggressive liveness probe that restarts a merely-slow app (crash loop), no readiness probe so rollouts send traffic to not-ready Pods, and pointing liveness at a deep dependency (a DB blip restarts your app needlessly — keep liveness shallow).",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      ports:\n        - containerPort: 8080\n      startupProbe:        # gives a slow app up to 30s to boot\n        httpGet: { path: /healthz, port: 8080 }\n        failureThreshold: 30\n        periodSeconds: 1\n      livenessProbe:       # restart if it wedges\n        httpGet: { path: /healthz, port: 8080 }\n        periodSeconds: 10\n      readinessProbe:      # gate traffic until ready\n        httpGet: { path: /ready, port: 8080 }\n        periodSeconds: 5\n```",
    "resources": [
      { "label": "Kubernetes docs — Liveness, Readiness and Startup Probes", "url": "https://kubernetes.io/docs/concepts/configuration/liveness-readiness-startup-probes/" }
    ]
  },
  "docker-k8s-health-mcq-001": {
    "explanation": "**The problem:** An overloaded Pod should stop receiving new requests to recover — but restarting it would drop in-flight work and worsen a thundering-herd. **Why readiness is the answer:** a failing readiness probe makes the endpoints controller remove the Pod from its Service's endpoint list, so new traffic stops flowing to it, while the container keeps running and can recover — then readiness passes again and traffic resumes. **Why not liveness:** a failing liveness probe restarts the container, which is wrong for transient overload (it throws away the process and any warm state). **Why not startup:** the startup probe only governs the initial boot window, not steady-state load shedding. **The takeaway:** readiness = traffic gating / graceful load shedding; liveness = recover a truly broken process. This separation is the whole reason the two probes exist. **Common mistakes:** wiring overload detection into liveness (causing restart storms), and making readiness depend on a downstream service so a dependency blip drains all your Pods at once.",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      readinessProbe:        # fails under load -> removed from endpoints, NOT killed\n        httpGet: { path: /ready, port: 8080 }\n        periodSeconds: 5\n        failureThreshold: 3\n```",
    "resources": [
      { "label": "Kubernetes docs — Configure readiness probes", "url": "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/#define-readiness-probes" }
    ]
  },
  "docker-k8s-scale-001": {
    "explanation": "**The problem:** Without resource hints the scheduler can't place Pods sensibly, and one greedy Pod can starve its neighbors or take down a node. **Requests:** the amount of CPU/memory a container is *guaranteed*; the scheduler reserves it and only places the Pod on a node with enough unreserved capacity. **Limits:** the *ceiling* a container may use — exceeding the **memory** limit gets the container **OOM-killed** and restarted; exceeding the **CPU** limit gets it **throttled** (slowed, not killed), because CPU is compressible and memory is not. **QoS classes:** requests/limits determine Guaranteed / Burstable / BestEffort, which decides eviction order under node pressure. **Why both matter:** requests drive scheduling and fairness; limits contain blast radius. **Common mistakes:** setting requests far below real usage (node overcommit → evictions), omitting memory limits (one leak OOMs the node), and setting CPU limits too tight (latency from throttling). On EKS Fargate, requests effectively size the micro-VM you pay for.",
    "example": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: app\n      image: myrepo/app:1.2.0\n      resources:\n        requests:           # guaranteed + used for scheduling\n          cpu: \"250m\"\n          memory: \"256Mi\"\n        limits:             # ceiling: over memory -> OOMKilled; over cpu -> throttled\n          cpu: \"500m\"\n          memory: \"512Mi\"\n```",
    "resources": [
      { "label": "Kubernetes docs — Resource management for Pods and Containers", "url": "https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/" }
    ]
  },
  "docker-k8s-scale-002": {
    "explanation": "**The problem:** Traffic is spiky, and running a fixed replica count either wastes money at the trough or falls over at the peak. **What the HPA does:** it automatically adjusts a Deployment's `replicas` up or down to keep an observed metric near a target — most commonly average CPU/memory utilization, but also custom or external metrics. **What it needs:** the **metrics-server** (or a custom-metrics adapter) installed so it can read Pod metrics, and the Pods must declare resource **requests** so 'utilization' (usage ÷ request) is well-defined. **How it scales:** it's *horizontal* — more/fewer Pod replicas; contrast the Vertical Pod Autoscaler (resizes each Pod) and the Cluster Autoscaler/Karpenter (adds/removes nodes). **On EKS these compose:** HPA adds Pods, and when there's no room a node autoscaler adds EC2 capacity (or Fargate runs each Pod on its own micro-VM). **Common mistakes:** no metrics-server (HPA shows `<unknown>` and won't scale), no resource requests (utilization can't be computed), and flapping from a too-tight target without stabilization windows.",
    "example": "```yaml\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: web\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: web\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target:\n          type: Utilization\n          averageUtilization: 70   # add Pods to keep avg CPU ~70% of requests\n```",
    "resources": [
      { "label": "Kubernetes docs — Horizontal Pod Autoscaling", "url": "https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/" }
    ]
  },
  "docker-k8s-scale-003": {
    "explanation": "**The problem:** Many teams and environments share one cluster, and without partitioning their names collide and everyone can touch everything. **What namespaces are:** virtual sub-clusters that scope the names of namespaced objects (Deployments, Services, ConfigMaps…) so `web` in `dev` and `web` in `prod` coexist. **What they enable:** per-namespace **ResourceQuotas** and **LimitRanges** (cap a team's consumption), **RBAC** (grant access per namespace), and **NetworkPolicies** (restrict cross-namespace traffic); they also scope DNS (`<svc>.<namespace>.svc.cluster.local`). **What's NOT namespaced:** cluster-scoped objects like Nodes, PersistentVolumes, and StorageClasses live outside any namespace. **The caveat:** namespaces are an organizational and soft-security boundary, not hard multi-tenant isolation by themselves — combine with RBAC, quotas, and network policies for real separation. **Common mistakes:** treating namespaces as a security sandbox without RBAC/NetworkPolicy, and forgetting `-n <namespace>` so `kubectl` operates on `default` and you 'lose' your resources.",
    "example": "```bash\nkubectl create namespace staging\n\n# Deploy into a namespace\nkubectl apply -n staging -f deployment.yaml\nkubectl get pods -n staging\n\n# Cap the namespace's total resource usage\nkubectl create quota team-quota -n staging \\\n  --hard=requests.cpu=4,requests.memory=8Gi,pods=20\n```",
    "resources": [
      { "label": "Kubernetes docs — Namespaces", "url": "https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/" }
    ]
  }
}
